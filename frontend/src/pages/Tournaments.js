import React from 'react';
import Navigation from '../components/Navigation';

const Tournaments = () => {
    return (
        <div>
            <Navigation />
            <h1>Mes Tournois</h1>
        </div>
    );
};

export default Tournaments;