import React from 'react';
import Navigation from '../components/Navigation';

const Players = () => {
    return (
        <div>
            <Navigation />
            <h1>Mes Joueurs</h1>
        </div>
    );
};

export default Players;